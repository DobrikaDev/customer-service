apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
        - name: customer-service
          image: docker.io/slipneff/dobrika-customer-service:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 8082
          volumeMounts:
            - name: config
              mountPath: /app/deployments/config.yaml
              subPath: config.yaml
      volumes:
        - name: config
          configMap:
            name: customer-service-config
            items:
              - key: config.yaml
                path: config.yaml

